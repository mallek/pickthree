/** Injected by vite.config.ts at build time: short git sha in CI, "dev" locally. */
declare const __PICK3_BUILD__: string;
/** Injected by vite.config.ts at build time: ISO timestamp of the build. */
declare const __PICK3_BUILT_AT__: string;
